import { IncidentDetailsPipe } from './incident-details.pipe';

describe('IncidentDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new IncidentDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
