import { ReportSubCategory } from './report-sub-category';

describe('ReportSubCategory', () => {
  it('should create an instance', () => {
    expect(new ReportSubCategory()).toBeTruthy();
  });
});
