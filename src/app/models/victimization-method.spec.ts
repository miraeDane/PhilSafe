import { VictimizationMethod } from './victimization-method';

describe('VictimizationMethod', () => {
  it('should create an instance', () => {
    expect(new VictimizationMethod()).toBeTruthy();
  });
});
